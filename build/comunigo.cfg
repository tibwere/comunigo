# This is a config file for comunigo app
# Some settings (chat group size, verbose 
# output on log and type of service offered)
# are run-time configured running startup.bash

WS_PORT=80
REG_PORT=29290
CHAT_PORT=29291

# Those lines should not be edited unless you want to port
# this project on a real cluster. In this case there you need
# to put IP addresses of those nodes
REG_HOSTNAME=registration_ms
SEQ_HOSTNAME=sequencer_ms
REDIS_HOSTNAME=peer_ds